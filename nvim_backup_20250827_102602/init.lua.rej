diff a/nvim/init.lua b/nvim/init.lua	(rejected hunks)
@@ -101,8 +101,7 @@ require("mason-lspconfig").setup({
   ensure_installed = {
     "lua_ls",        -- Lua
     "pyright",       -- Python
-    -- "typescript-language-server" 또는 "tsserver"가 아니라 Mason에서는 lspconfig 이름을 사용
     -- TypeScript/JavaScript는 LSP 설정만 유지하고 자동 설치에서 제외
     "rust_analyzer", -- Rust
     "clangd",        -- C/C++
     "marksman",      -- Markdown
